# Issue #111 — Voting completion: reduce sense of incompleteness when statement queues are large

**Verdict:** CONTRADICTS/OBSOLETE (scoped to research only — implementation explicitly out of scope)

## Reason

The issue's own text states: "Implementation. This issue is resolved once we have a clear, evidence-backed design recommendation. Implementation is a separate issue to be filed after."

This issue is a research/design task, not an engineering task. No implementation plan can be written until the literature review produces a design recommendation. There is no code to change.

## What would trigger a FITS verdict

A follow-on issue filed after the literature review that specifies the chosen UX approach (checkpoint interstitial, hard cap, soft opt-in continuation screen, etc.) and asks for implementation. That issue should reference the research output.

## If a literature review is desired now

This is out of scope for a code-only implementation plan. The research could be performed via the `deep-research` skill and its output stored in `v2/.claude/research-111-voting-completion.md`.
